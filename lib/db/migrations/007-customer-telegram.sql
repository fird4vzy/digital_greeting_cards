-- Телеграм заказчика.
--
-- В Узбекистане связываются в телеграме, а почту не читают: форма просила
-- «телефон или почту», и второе поле стояло мёртвым. Салону нужен канал, по
-- которому человек действительно ответит.
--
-- Почта остаётся колонкой и не удаляется: в уже принятых заказах она есть, и
-- терять контакт живого клиента ради чистоты схемы нельзя. Просто её больше
-- не спрашивают.

ALTER TABLE orders ADD COLUMN IF NOT EXISTS customer_telegram TEXT;
