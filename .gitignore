node_modules
.next
out
build
dist

.env
.env*.local

.DS_Store
*.pem

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

next-env.d.ts
tsconfig.tsbuildinfo

# local file-backed store used when no DATABASE_URL is configured
.data/

# local handoff artefacts, not part of the project
*.bundle
*.tar.gz
*.jsonl.gz
dev.log
dev.err.log
LOCALSETUP.md
# Дописано `vercel link`: здесь лежат идентификаторы проекта.
.vercel

# Ключи. `.env*` шире, чем `.env*.local`, намеренно: `.env.local.bak` —
# копия боевого файла, и под узкий шаблон она не попадала.
.env*
!.env.example
