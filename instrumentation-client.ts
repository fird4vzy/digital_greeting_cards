import * as Sentry from '@sentry/nextjs';

/**
 * Sentry на клиенте.
 *
 * **Запись сессий выключена, и это не настройка на будущее.** Мастер включает
 * `replayIntegration` с выборкой 10% всех сессий: это видеозапись экрана —
 * DOM и ввод, — которая уезжает на серверы Sentry в Германии. Единственный
 * экран, где человек что-то печатает, у нас `/create`: там пишут письмо
 * любимому человеку и загружают семейные фотографии. Записывать это нельзя ни
 * при какой выборке, а маскировать полей столько, что одна пропущенная
 * означает утечку самого личного, что в продукте есть.
 *
 * По той же причине выключены `userInfo` и `httpBodies`: тело запроса к
 * `/api/orders` — это имя, телефон, телеграм, текст письма и фотографии в
 * base64. Ошибка не должна тащить их за собой в трекер.
 *
 * Выборка трассировок в проде 10%, а не 100%: бесплатная квота Sentry уходит
 * за день, а для «падает ли и как часто» десятой части хватает с запасом.
 */


Sentry.init({
  dsn: "https://4635933e0deddf749475ea1d50877331@o4511988297826304.ingest.de.sentry.io/4511988301758544",

  tracesSampleRate: process.env.NODE_ENV === 'production' ? 0.1 : 1,

  dataCollection: {
    userInfo: false,
    httpBodies: [],
  },
});

export const onRouterTransitionStart = Sentry.captureRouterTransitionStart;
